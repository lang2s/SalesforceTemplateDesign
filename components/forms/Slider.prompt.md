**SLDS Slider** — a range input with brand-filled track and value label.

```jsx
<Slider label="Probability" min={0} max={100} step={5} defaultValue={75} unit="%" />
```

Props: `label`, `min`/`max`/`step`, `value`/`defaultValue`, `onChange`, `showValue`, `unit`, `disabled`.

**SLDS Textarea** — a multi-line text input matching SLDS form styling.

```jsx
<Textarea label="Description" rows={4} placeholder="Add details…" />
```

Props: `label`, `value`/`defaultValue`, `onChange`, `rows`, `required`, `disabled`, `error`.

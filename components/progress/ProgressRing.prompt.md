**SLDS Progress Ring** — a compact circular completion indicator (0–100).

```jsx
<ProgressRing value={75} size={40} showValue />
<ProgressRing value={100} variant="success" />
```

Props: `value`, `size` (px), `variant` (`base`·`success`·`warning`·`error`), `showValue`.

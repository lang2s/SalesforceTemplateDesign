**SLDS Helptext** — a small info icon revealing a dark tooltip on hover/focus; pair with field labels.

```jsx
<label>Annual Revenue <Helptext content="Reported revenue for the last fiscal year." /></label>
```

Props: `content`, `iconName`, `align` (`top`·`bottom`·`left`·`right`). Also serves as the general Tooltip primitive.

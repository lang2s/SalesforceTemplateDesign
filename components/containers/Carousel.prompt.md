**SLDS Carousel** — a sliding panel with indicator dots + arrows.

```jsx
<Carousel slides={[
  { content: <Panel a/> },
  { content: <Panel b/> },
]} height={240} />
```

Props: `slides` (`{content}` or nodes), `height`, `showArrows`. Active dot stretches to a pill.
